# Simulink / Stateflow build caches (regenerated automatically, never hand-edited)
slprj/
**/slprj/
*.slxc

# MATLAB autosave / temp
*.asv
*.autosave

# Generated code (if code generation is ever run here)
codegen/
*.mex*

# OS junk
Thumbs.db
desktop.ini
$RECYCLE.BIN/

# Claude Code local, machine-specific settings and session lock files (not shared project config)
.claude/settings.local.json
.claude/*.lock

# Editor/temp
*.tmp
~$*
